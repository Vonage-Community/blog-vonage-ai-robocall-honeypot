{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "Robo Call blocker",
  "version": "1.0.0",
  "description": "A Demo express server which is the companion for \"Building a Robocaller honeypot with Vonage AI studio\"",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "watch": "nodemon --watch src --exec node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "name": "Chuck \"MANCHUCK\" Reeves",
    "email": "chuck@manchuck.com"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "dotenv": "16.3.1",
    "express": "4.18.2"
  }
}
